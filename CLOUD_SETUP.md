# الحساب وحفظ الشركات

تستخدم V2 مشروع Firebase نفسه المستخدم في مسراح، مع Google والبريد وكلمة المرور وإنشاء الحساب واستعادة كلمة المرور.

## الإعداد

1. يحمل الملف `v2/firebase-config.js` إعدادات تطبيق الويب العامة لمشروع `mesraah-a2dfc`.
2. يجب أن يكون النطاق `almohammdin.github.io` ضمن Authorized domains في Firebase Authentication. المعاينة المحلية قد تحتاج اعتماد عنوانها على حدة.
3. يجب تفعيل Google وEmail/Password في Authentication.
4. يخزن المصمم بياناته في `users/{uid}.sjscCapitalDesigner.companies`. تظل بيانات مسراح في حقل `mesraah` مستقلة.
5. يجب أن تسمح قواعد Firestore لصاحب الحساب فقط بقراءة مستنده وتعديله. راجع القواعد الموجودة قبل أي تغيير؛ لا تستبدل قواعد المشروع المشترك تلقائيا.

الإعدادات المنشورة هي معرفات تطبيق عميل عامة. الحماية تعتمد على المصادقة وقواعد Firestore. مفاتيح حساب الخدمة وكلمات المرور والتوكنات الخاصة تبقى خارج المستودع والواجهة.

## الحفظ

زر «حفظ الشركة» يحتفظ بنسخة على الجهاز أولا، ثم يرسلها إلى الحساب عند تسجيل الدخول. تظهر نتيجة المزامنة بشكل مستقل عن نجاح الدخول. يستخدم التخزين المحلي مساحة خاصة بكل مستخدم، ومساحة مستقلة للزائر. يمكن فتح ملف زائر ثم تسجيل الدخول وحفظه لنقله إلى الحساب.

تستخدم عمليات كتابة الشركات معاملات Firestore لدمج السجلات حسب المعرف وتاريخ التعديل، دون حذف شركات أخرى عند الحفظ. يظل أحدث تعديل محفوظ لكل شركة هو المعتمد عند اختلاف النسخ. عند فشل الشبكة أو امتلاء المستند تبقى النسخة المحلية ويظهر فشل المزامنة.

## المشاركة

زر المشاركة ينشئ لقطة مستقلة داخل جزء fragment من الرابط. يستطيع حامل الرابط فتح محتواها. لا ينشئ سجلا عاما أو يغير صلاحيات قاعدة البيانات، ولا تتحدث اللقطة تلقائيا بعد تعديل الشركة الأصلية. لا يمكن إلغاء نسخة رابط سبق مشاركته. حجم الرابط يرتبط بكمية البيانات.

ملفات Supabase القديمة في نسخة العمل مرجع سابق، ولا تحملها الواجهة المنشورة.

## التحقق

التحقق البرمجي والمحلي يغطي العرض والحسابات والحفظ المحلي والمشاركة وربط أحداث الدخول. إثبات الحفظ السحابي من طرف إلى طرف يتطلب جلسة حساب حقيقية وقواعد Firestore المطبقة في المشروع.
